pipeline {
  agent {
    docker {
      image 'maven:3.8.1-adoptopenjdk-11'
    }
  }
  stages {
    stage('Build') {
      steps {
        sh 'mvn -Dmaven.repo.local=/root/.m2 -B test'
      }
    }
  }
  post {
    always {
      deleteDir()
    }
  }
}