pipeline {
  agent {
    docker {
      image 'maven:3.8.1-adoptopenjdk-11'
      args '-v /home/vagrant/.m2:/root/.m2'
    }
  }
  stages {
    stage('Build') {
      steps {
        sh 'mvn -X -B test'
      }
    }
  }
  post {
    always {
      deleteDir()
    }
  }
}